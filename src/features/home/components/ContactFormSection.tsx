import { ContactForm } from './ContactForm'

export const ContactFormSection = () => (
  <section class="layout-section items-center gap-12">
    <h2 class="font-main text-center text-3xl font-medium text-balance">
      Send some test message
    </h2>
    <ContactForm />
  </section>
)
